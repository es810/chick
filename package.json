{
  "name": "chick",
  "version": "1.0.0",
  "private": true,
  "description": "Chicken Farm monorepo — API deploy uses /server or root Dockerfile",
  "scripts": {
    "start": "npm start --prefix server",
    "postinstall": "npm ci --omit=dev --prefix server || npm install --omit=dev --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
